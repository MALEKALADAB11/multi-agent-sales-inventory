\chapter*{Liste des abréviations}

%=============== Liste triée automatiquement ==============%
% L'environnement `acronyms` du template trie les entrées   %
% par label. Une entrée = \sortitem[SIGLE]{Développement}.  %
%==========================================================%

\begin{acronyms}

    \sortitem[API]{Interface de programmation applicative (\textit{Application Programming Interface})}
    \sortitem[B2B]{Commerce interentreprises (\textit{Business to Business})}
    \sortitem[B2C]{Commerce vers le particulier (\textit{Business to Consumer})}
    \sortitem[BF]{Besoin fonctionnel}
    \sortitem[BNF]{Besoin non fonctionnel}
    \sortitem[CA]{Chiffre d'affaires}
    \sortitem[CI/CD]{Intégration et déploiement continus (\textit{Continuous Integration / Continuous Deployment})}
    \sortitem[CRISP-DM]{Processus standard intersectoriel pour la fouille de données (\textit{Cross-Industry Standard Process for Data Mining})}
    \sortitem[CRM]{Gestion de la relation client (\textit{Customer Relationship Management})}
    \sortitem[CSM]{Critère de succès métier}
    \sortitem[EOQ]{Quantité économique de commande (\textit{Economic Order Quantity})}
    \sortitem[GIMSI]{Généralisation, Information, Méthode et Mesure, Système et Systémique, Individualité, Initiative}
    \sortitem[HITL]{Humain dans la boucle (\textit{Human In The Loop})}
    \sortitem[IA]{Intelligence artificielle}
    \sortitem[ISO]{Organisation internationale de normalisation (\textit{International Organization for Standardization})}
    \sortitem[JSON]{Notation d'objet JavaScript (\textit{JavaScript Object Notation})}
    \sortitem[KDD]{Extraction de connaissances à partir de données (\textit{Knowledge Discovery in Databases})}
    \sortitem[KPI]{Indicateur clé de performance (\textit{Key Performance Indicator})}
    \sortitem[LLM]{Grand modèle de langage (\textit{Large Language Model})}
    \sortitem[MAE]{Erreur absolue moyenne (\textit{Mean Absolute Error})}
    \sortitem[MASE]{Erreur absolue moyenne rapportée à une référence naïve (\textit{Mean Absolute Scaled Error})}
    \sortitem[MCP]{Protocole de contexte de modèle (\textit{Model Context Protocol})}
    \sortitem[MoSCoW]{Priorisation : \textit{Must}, \textit{Should}, \textit{Could}, \textit{Won't have}}
    \sortitem[MOQ]{Quantité minimale de commande (\textit{Minimum Order Quantity})}
    \sortitem[MSTL]{Décomposition saisonnière multiple par lissage local (\textit{Multiple Seasonal-Trend decomposition using Loess})}
    \sortitem[OD]{Objectif de fouille de données}
    \sortitem[OM]{Objectif métier}
    \sortitem[PFE]{Projet de fin d'études}
    \sortitem[POS]{Point de vente (\textit{Point Of Sale})}
    \sortitem[RAG]{Génération augmentée par la recherche (\textit{Retrieval-Augmented Generation})}
    \sortitem[RBAC]{Contrôle d'accès fondé sur les rôles (\textit{Role-Based Access Control})}
    \sortitem[REST]{Transfert d'état représentationnel (\textit{Representational State Transfer})}
    \sortitem[RMSE]{Racine de l'erreur quadratique moyenne (\textit{Root Mean Square Error})}
    \sortitem[ROP]{Point de commande (\textit{Reorder Point})}
    \sortitem[SEMMA]{\textit{Sample, Explore, Modify, Model, Assess}}
    \sortitem[SKU]{Référence produit (\textit{Stock Keeping Unit})}
    \sortitem[SMA]{Système multi-agents}
    \sortitem[SQL]{Langage de requête structuré (\textit{Structured Query Language})}
    \sortitem[SS]{Stock de sécurité (\textit{Safety Stock})}
    \sortitem[SSE]{Événements envoyés par le serveur (\textit{Server-Sent Events})}
    \sortitem[TDSP]{Processus de science des données en équipe (\textit{Team Data Science Process})}
    \sortitem[TND]{Dinar tunisien}
    \sortitem[UML]{Langage de modélisation unifié (\textit{Unified Modeling Language})}
    \sortitem[WAPE]{Erreur absolue pondérée (\textit{Weighted Absolute Percentage Error})}
    \sortitem[WS]{Connexion bidirectionnelle persistante (\textit{WebSocket})}
    \sortitem[XGBoost]{Renforcement de gradient extrême (\textit{Extreme Gradient Boosting})}

\end{acronyms}

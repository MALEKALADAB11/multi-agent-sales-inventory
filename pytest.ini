[pytest]
testpaths = tests
python_files = test_*.py
markers =
    db: nécessite la base PostgreSQL ooredoo_sales locale
    integration: nécessite le serveur (uvicorn main:app) et/ou l'infra Docker
addopts = -q
